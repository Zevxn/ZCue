<Window x:Class="TypeSense.Views.PromptManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TypeSense Prompt 管理器"
        Width="760"
        Height="500"
        MinWidth="620"
        MinHeight="360"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Prompt 管理器"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#1F2937" />
        <TextBlock Grid.Row="1"
                   Margin="0,8,0,18"
                   Text="当前为第一阶段 MVP，展示内置测试 Prompt；JSON 新增、修改、删除将在下一阶段接入。"
                   Foreground="#6B7280"
                   FontSize="13" />

        <ListView Grid.Row="2"
                  ItemsSource="{Binding Prompts}"
                  BorderBrush="#E5E7EB"
                  BorderThickness="1"
                  Padding="4">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="名称" Width="130" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="缩写" Width="100" DisplayMemberBinding="{Binding Abbreviation}" />
                    <GridViewColumn Header="使用次数" Width="80" DisplayMemberBinding="{Binding UsageCount}" />
                    <GridViewColumn Header="内容" Width="390" DisplayMemberBinding="{Binding Content}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
